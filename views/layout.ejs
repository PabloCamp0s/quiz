<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>Preguntas</title>
  <link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/wide.css"
        media="only screen and (min-width: 530px) and (min-device-width: 481px)" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/smartphone.css"
        media="only screen and (max-width: 480px)" />
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
</head>

<body>
<div id="page-wrap">
  <header class="main" id="h1">
    <% if ( !session.user ) { %>
    <span class="right"><a href="/login">Login</a></span>
    <% }else{ %>
    <form id="logout" method="post" action="/login?_method=delete"></form>
    <span class="right">
      <%= session.user.username %>
      <a href="#" onclick="$( 'form#logout' ).submit()">Logout</a>
    </span>
    <% } %>
    <h2>Quiz: el juego de las preguntas</h2>
  </header>

  <nav class="main" id="n1" role="navigation">
    <span><a href="/">Inicio</a></span>
    <span><a href="/quizes">Preguntas</a></span>
    <span><a href="/author">Créditos</a></span>
  </nav>

  <section class="main" id="s1">
    <% if (errors.length){ %>
    <div id="errors">
      <div>
        Corregir errores:
      </div>
      <% errors.forEach(
      function ( error )
      {
      %>
      <div><%= error.message %></div>
      <%
      }
      ) %>
    </div>
    <% } %>
    <%- body %>
  </section>

  <footer class="main" id="f1">
    <a href="http://es.wikipedia.org/wiki/Quiz">¿Qué es un Quiz?</a>
    <a href="https://github.com/PabloCamp0s/quiz">Mi proyecto en Github</a>
  </footer>
</div>
</body>
</html>